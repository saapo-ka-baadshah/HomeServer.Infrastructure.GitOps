# OpenCode Config

## Installation

Clone this repository into your current project's `.opencode` directory:

```bash
[ ! -d ".opencode" ] && git clone https://github.com/saapo-ka-baadshah/OpenCode.Config.git .opencode && rm -rf .opencode/.git && mv .opencode/opencode.json .
```
